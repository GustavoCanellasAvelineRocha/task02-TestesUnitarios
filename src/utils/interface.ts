export interface IChildren {
  children: React.ReactNode;
}

export interface Character {
  id: string;
  firstName: string;
  lastName: string;
  title: string;
  family: string;
  image: string;
  imageUrl: string;
}
